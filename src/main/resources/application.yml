spring:
  application:
    name: organizationservice
  jpa:
    hibernate:
      ddl-auto: none
#Setting the logging levels for the service
logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.diego: DEBUG
server:
  port: 8081
signing:
  key: 1234

